# Simulated deploy values
replicaCount: 1

image:
  repository: registry.example.com/sim-agent
  tag: "1.1.0-d6e1476"
  pullPolicy: IfNotPresent

service:
  type: ClusterIP
  port: 8080

resources:
  limits:
    cpu: 500m
    memory: 256Mi
  requests:
    cpu: 100m
    memory: 128Mi
